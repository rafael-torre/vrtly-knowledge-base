# Step 1: Validate Prerequisites

**Actions:**
- Locate Layer 3 feature technical spec: layers/layer-3-architecture/final/features/<domain>/<name>-technical.md
- Confirm status: consensus in frontmatter
- Load Layer 3 architecture overview for cross-cutting constraints
- Read .companion.yaml board section: check board.type and board.project_id are set
- Halt if spec not in final/ or board not configured
